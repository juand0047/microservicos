server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: develop
          uri: https://github.com/juand0047/microservicos.git
          search-paths: config-data
  application:
    name: config-service
